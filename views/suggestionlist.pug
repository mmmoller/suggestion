extends layout/in

block content

    include mixin/suggestionTable
    +Suggestiontable(undefined, suggestion, infosys, ownUser)

    if (showCreateSuggestion)
        include mixin/createSuggestion
        +CreateSuggestion(infosys)